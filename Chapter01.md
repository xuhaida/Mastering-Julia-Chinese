2009年以来的这段时期，出现了2个新的计算领域，大数据/云计算和数据科学。Hadoop上的大数据处理通常被视为Java编程的领域。因为Hadoop运行于Java虚拟机之上。当然，可以使用Java以外的语言来处理大数据。使用流.jar范型，Julia被用于一种与C++，C#，python类似的方式进行处理。

数据科学的出现驱动着编程语言的使用，对于分析师而言具有一些编程技巧，却又不是主要是程序员。两种语言来填补了这个鸿沟：R和python。这两种语言都相对比较老，可以追溯至20世纪90年代。但是，这两种语言的流行可以视为快速成长，具有讽刺意味的时间，Julia开始，即使如此，Julia激动这科学计算社区和在这个空间里走出自己的道路。

本书的目的是覆盖Julia的方方面面，使其对数据科学家更具吸引力。语言进化很快，二进制发布适用于Linux，Windows和Mac OSX。但是这些，因此，使用Julia做一些认真的工作是，重要的是理解如何从源代码中获得和构建。另外Julia具有可交互的开发环境。本书中会同时讨论Jupyter和Juno IDE.

**Julia编程环境**

本章中，我们将探索所有读者需要初始化Julia需要的所有知识，从源代码构建至获取预编译好的二进制。Julia也可以在Juno中的顺带下载。Julia可以使用IPython运行。Ipython发布在网上，通过https://juliabox.org/网址。Julia是高级的高性能动态语言。用于科学计算。它可以运行于Linux，OSX和Windows。

下载稳定构建版本和发行在。因此，从源代码构建并非重要。在这些，但是，既让Julia为开源软件，从Julia语言的官方网站获取详细的说明信息。

在Mac OSX上，读者需要用到64位gfortran编译器来构建Julia，

**探索源代码栈**

深入了解Julia，最好的需要浏览的目录是base，example和test。

1. 在base目录中包含了一大部分标准库，编码例子风格是清晰的。
2. test目录包含一些目录来说明如何写测试脚本和使用Base.test.
3. examples目录给遇Julia一些有名的老的计算系统。皇后问题，词汇计数和生命游戏。

如果读者从源代码构建Julia，会获得Git/build目录下所有的目录，构建过程会创建一个新目录usr，可执行目录放置于usr/bin。

IJulia为Julia语言的后台接口，使用IPython交互环境，现在是Juypter的一部分，一个通用语言中立的平台，通用于其他编程语言。这种组合允许读者与Julia语言，通过使用IPython强大的图形笔记本，组合了代码。格式化文本，数据支持，还有就是单个文件的多媒体。

读者需要安装1.0版或者更高版本的IPython，注意IPython 1.0是在2013年8月份发布，因此该版本需要用2.7版 python，而操作系统中预安装的版本会太老，运行不了。如果是这样，读者可以手动安装IPython。

在Mac OSX和Windows系统中，最方便的方法是使用Anaconda Python安装包，安装完Anaconda后，使用conda命令来安装IPYhon。

        conda update cond

在Ubuntu中，我们使用apt-get命令：

**迅速浏览一下Julia**

来电开胃的，我们来看一个例子，使用随机数来定价亚洲生化

股份是有权去购入特殊的股票，在一定价格时即允许，人员从中收益，是受益方，选择成熟了，受益人也许会选择练习一下，如果达到他/她的收益度，去达成交易。

为了设置交易，受益人必须先

**从终端中运行Julia**

我们可以生成文件asian.jl，使用如下的代码：

        function run_(N=10000,putCall='C')

**Julia语言开发**

Julia具有丰富的特性，设计用来吸引新的编程人员和纯粹者，特别是对兴趣于数据科学，统计学和数学建模。Julia有他们需要的所有设备。

我们的目标是装配读者必须的知识来开始编程。我们不对开始对所有语法进行浏览。控制结构。我们必须Julia：在后面2章里，我们会浏览一下许多基本和高级特性。Julia用于作图和数据库的链接接口。通过软件包系统加以实现，留你后。

如果读者熟悉Python，R，Matlab，就能发现这个旅程并不可怕。实际上，我们相信这个过程是特别愉快。现在，Julia还没有发布1.0版，其中许多会depracated。并在后面发生变化。但是，我们相信这里讨论的一切是经得起时间考验的。

**整数，比特，字节**

Julia是强类型语言，允许编程人员指定数据的类型。但是，与大多数解释性语言一样，类型定义不是必须的。而是从定义的形式中推断。

Julia中的变量可以由大小写字母，数字，下划线和感叹号（！）等组成。必须以字母或者下划线开始。习惯上，变量名称有小写字母用下划线分开，而不是驼峰记法。我们用typeof（）函数来判定其变量类型。

因此，经常

注意，类型以大写字符开始，以数字结尾，表明变量的字节长度和位数。默认是操作系统的字符长度。可以使用内置变量word——size来检查判定。

在本节里，我们将会处理整数和布尔类型。

**整数**

整数类型可以使Int8，Int16，Int32，Int64，和Int128.因此最大的整数会占用16个字节用于存储。范围在。如果我们需要更高精度，Julia核心实现中BigInt

关于整数类型需要说明：

除了整型，Julia还提供了无符号数据类型Uint。同样的，Uint范围从8～128字节。因此，Uint的最大值为。我们可以输入typenan()和typenon()来输出Int和Uint类型的范围。
